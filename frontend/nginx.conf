server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # Le navigateur ne parle qu'a ce nginx : pas de CORS a configurer cote Spring Boot.
    location /api/ {
        proxy_pass http://backend:8080/api/;
    }
}
